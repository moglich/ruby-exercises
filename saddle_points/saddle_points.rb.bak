class Matrix

  attr_accessor :rows, :columns

  def initialize(matrix)
    @rows = Array.new
    @rows = matrix.split("\n").map(&:split)
    @rows.each do |row|
      row.map!(&:to_i)
    end
    tmp_row = @rows
    @columns = tmp_row.each do |row|
      row.pop
    end
  end
end
